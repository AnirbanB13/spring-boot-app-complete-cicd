apiVersion: v1
kind: Service
metadata:
  name: springbootapp-service 
  labels:
    app: springbootapp
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: springbootapp
  type: NodePort